# Python 3.8 호환 공통 라이브러리
# Callytics 마이크로서비스 공통 의존성
# Python 3.8 안정성 우선, 호환성 검증된 버전

# =============================================================================
# 웹 프레임워크 및 API
# =============================================================================
# FastAPI - Python 3.8 호환 안정 버전
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.6.4
pydantic-settings==2.0.3

# =============================================================================
# 데이터베이스 및 ORM
# =============================================================================
# PostgreSQL
psycopg2-binary==2.9.9
asyncpg==0.29.0

# SQLite (개발용)
aiosqlite==0.19.0

# ORM
sqlalchemy==1.4.53
alembic==1.12.1

# =============================================================================
# HTTP 클라이언트 및 통신
# =============================================================================
# HTTP 클라이언트
httpx==0.25.2
aiohttp==3.9.1
requests==2.31.0

# 메시지 큐
redis==5.0.1
celery==5.3.4

# =============================================================================
# 데이터 처리 및 분석
# =============================================================================
# 수치 계산 - Python 3.8 안정 버전
numpy==1.24.3
scipy==1.10.1
pandas==2.0.3

# 머신러닝
scikit-learn==1.3.2
joblib==1.3.2

# =============================================================================
# 자연어 처리
# =============================================================================
# 한국어 처리
konlpy==0.6.0
soynlp==0.0.493
hgtk==0.1.0

# 텍스트 처리
nltk==3.8.1
spacy==3.7.2
textblob==0.17.1

# =============================================================================
# 로깅 및 모니터링
# =============================================================================
# 로깅
structlog==23.2.0
python-json-logger==2.0.7

# 모니터링
prometheus-client==0.19.0
psutil==5.9.6

# =============================================================================
# 유틸리티 및 도구
# =============================================================================
# 환경 변수
python-dotenv==1.0.0

# 설정 관리
pyyaml==6.0.1
configparser==6.0.0

# 날짜/시간
python-dateutil==2.8.2
pytz==2023.3

# 파일 처리
watchdog==3.0.0

# =============================================================================
# 테스트 및 개발
# =============================================================================
# 테스트
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# 코드 품질
black==23.11.0
flake8==6.1.0
mypy==1.7.1

# =============================================================================
# 보안 및 인증
# =============================================================================
# 암호화
cryptography==41.0.7
passlib==1.7.4

# JWT
python-jose[cryptography]==3.3.0
python-multipart==0.0.6

# =============================================================================
# 성능 최적화
# =============================================================================
# 비동기 처리
asyncio-mqtt==0.16.1
aiofiles==23.2.1

# 캐싱
cachetools==5.3.2

# =============================================================================
# 오디오 처리 (Python 3.8 호환)
# =============================================================================
# 오디오 파일 처리
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1

# 음성 인식
speechrecognition==3.10.0
pocketsphinx==0.1.15

# =============================================================================
# GPU 지원 (선택적)
# =============================================================================
# PyTorch - Python 3.8 호환
torch==2.1.1
torchaudio==2.1.1
torchvision==0.16.1

# TensorFlow - Python 3.8 호환
# tensorflow==2.13.1

# =============================================================================
# 한국어 특화 라이브러리
# =============================================================================
# 한국어 형태소 분석
mecab-python3==1.0.6

# =============================================================================
# 버전 호환성 확인
# =============================================================================
# Python 3.8 호환성 검증 완료
# 모든 라이브러리가 Python 3.8에서 안정적으로 동작

six==1.16.0
# Python 3.8 이상에서는 pathlib 표준 포함 -> pathlib2 제거 

# typing-extensions for TF requirement
typing-extensions==4.9.0

# async-timeout 4.0.3
async-timeout==4.0.3

# =============================================================================
# 추가 고정 버전 (의존성 빌드 오류 방지)
# =============================================================================
# 최신 tokenizers 0.2x 버전은 Python 3.9+ wheel만 제공해 3.8에서 소스 빌드가 발생하며
# Rust toolchain 및 puccinialin 빌드 의존성 때문에 실패합니다.
# 0.14.1 버전은 transformers 4.35.x 와 호환되고 CP38 manylinux wheel을 제공하므로 고정합니다.
tokenizers==0.14.1

# Transformers 버전 고정: 최신 4.5x 는 tokenizers 0.21+ 만 요구해 Python 3.8 빌드가 실패하므로
# 4.35.2 (LTS 안정) 버전으로 고정하여 tokenizers 0.14.1 과 호환되게 한다.
transformers==4.35.2 