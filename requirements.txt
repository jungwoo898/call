# Core dependencies (검증된 안정 버전)
numpy>=1.24.0,<2.0.0
scipy==1.11.4
requests==2.31.0
python-dotenv==1.0.0
pyyaml==6.0.1
omegaconf==2.3.0
hydra-core==1.3.2
watchdog[watchmedo]==3.0.0
pydub==0.25.1
soundfile==0.12.1
librosa==0.10.1
noisereduce==3.0.0
nltk==3.8.1
openai==1.3.7

# API dependencies (검증된 안정 버전)
aiohttp==3.9.1
httpx==0.25.2
huggingface-hub==0.19.4

# Web API dependencies (검증된 안정 버전)
pydantic==2.5.0
fastapi==0.104.1
uvicorn[standard]==0.24.0
psutil==5.9.6

# ML/AI dependencies (검증된 안정 버전)
accelerate==0.25.0
transformers==4.44.0
IPython==8.18.1
treetable==0.2.5

# GPU 가속 오디오 처리 (검증된 안정 버전)
faster-whisper==0.10.0
demucs==4.0.0
# mamba-ssm>=2.0.0,<3.0.0  # 사전 빌드된 wheel로 별도 설치
nemo_toolkit==1.20.1
pyannote.audio==3.1.1
MPSENet==1.0.3
ctc-forced-aligner==1.0.2

# Language detection
langdetect==1.0.9

# Punctuation restoration
deepmultilingualpunctuation==1.0.1

# Additional dependencies (검증된 안정 버전)
pandas==2.1.4
matplotlib==3.8.2
scikit-learn==1.3.2
memory-profiler==0.61.0
cython==3.0.6

# 음성 처리 핵심 라이브러리 (검증된 안정 버전)
speechbrain==0.5.16

# 텍스트 처리 및 NLP (검증된 안정 버전)
datasets==2.16.1
tokenizers==0.19.3
sentencepiece==0.1.99
sentence-transformers==2.2.2

# 오디오 처리
webrtcvad==2.0.10

# AI 및 머신러닝 (검증된 안정 버전)
langchain==0.1.0
langchain-openai==0.0.2

# GPU 모니터링 도구
nvidia-ml-py==12.535.133
gpustat==1.1.1

# 개발 및 디버깅 (검증된 안정 버전)
pytest==7.4.3
black==23.11.0
isort==5.12.0

# 기타 유틸리티 (검증된 안정 버전)
tqdm==4.66.1
rich==13.7.0
click==8.1.7

# 주의: PyTorch GPU 버전은 Dockerfile에서 CUDA 11.8과 함께 설치됨
# 검증된 안정 버전 조합으로 호환성 최적화:
# ✅ PyTorch 2.1.2 + torchvision 0.16.2 + torchaudio 2.1.2 (CUDA 11.8 호환)
# ✅ numpy: 1.24.x (검증된 안정 버전)
# ✅ mamba-ssm: 2.2.2 사전 빌드 wheel (Dockerfile에서 별도 설치)
# ✅ demucs: 4.0.0 (PyTorch 2.1.2 호환)
# ✅ nemo_toolkit: 1.20.1 (PyTorch 2.1.2 호환)
# ✅ speechbrain: 0.5.16 (PyTorch 2.1.2 호환)
# ✅ transformers: 4.44.0 (실제 코드에서 AutoTokenizer, AutoModelForCausalLM 사용)
# ✅ tokenizers: 0.19.3 (transformers 4.44.0 호환)
# ✅ datasets: 2.16.1 (tokenizers 0.19.3 호환)
# ✅ accelerate: 0.25.0 (transformers 4.44.0 호환)
# ✅ pyannote.audio: 3.1.1 (PyTorch 2.1.2 호환)
# ✅ huggingface-hub: 0.19.4 (transformers 4.44.0 호환)
# ✅ FastAPI: 0.104.1 + Pydantic: 2.5.0 (실제 코드에서 광범위 사용)
# ✅ faster-whisper: 0.10.0 (실제 코드에서 사용)
# ✅ sentence-transformers: 2.2.2 (실제 코드에서 사용)
