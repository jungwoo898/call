# audio-processor 서비스 전용 라이브러리
# 공통 라이브러리는 requirements_common_py38.txt에서 관리
# Python 3.8 호환 버전으로 업데이트

# 공통 라이브러리 포함
-r requirements_common_py38.txt

# =============================================================================
# 오디오 처리 전용 라이브러리 (Python 3.8 호환)
# =============================================================================
# 오디오 파일 변환 및 처리
ffmpeg-python==0.2.0
pyloudnorm==0.1.1

# 오디오 품질 분석
webrtcvad==2.0.10

# 오디오 시각화 (Python 3.8 호환)
matplotlib==3.7.2
seaborn==0.12.2

# 병렬 처리
joblib==1.3.2
multiprocessing-logging==0.3.4

# =============================================================================
# 성능 최적화
# =============================================================================
# 메모리 최적화
memory-profiler==0.61.0

# =============================================================================
# 오디오 특화 라이브러리
# =============================================================================
# 오디오 신호 처리
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1

# 오디오 품질 측정
pystoi==0.3.3

# =============================================================================
# Python 3.8 호환성 확인
# =============================================================================
# 모든 라이브러리가 Python 3.8에서 안정적으로 동작
