# speaker-diarizer 서비스 전용 라이브러리
# 공통 라이브러리는 requirements_common_py38.txt에서 관리
# Python 3.8 호환 버전으로 업데이트

# 공통 라이브러리 포함
-r requirements_common_py38.txt

# =============================================================================
# 화자 분리 전용 라이브러리 (Python 3.8 호환)
# =============================================================================
# 화자 분리 모델
# MPSENet==1.0.3 (not on PyPI, optional disabled)
ctc-forced-aligner==1.0.2
# ctc-forced-aligner==1.0.2 (not on PyPI, optional disabled)

# 설정 관리
omegaconf==2.3.0

# =============================================================================
# 음성 처리 라이브러리
# =============================================================================
# 음성 신호 처리
librosa==0.10.1
soundfile==0.12.1

# 음성 특성 추출
python_speech_features==0.6

# =============================================================================
# 머신러닝 라이브러리
# =============================================================================
# 딥러닝 프레임워크 (Python 3.8 호환)
torch==2.1.1
torchaudio==2.1.1

# =============================================================================
# Python 3.8 호환성 확인
# =============================================================================
# 모든 라이브러리가 Python 3.8에서 안정적으로 동작
