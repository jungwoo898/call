# speech-recognizer 서비스 전용 라이브러리
# 공통 라이브러리는 requirements_common_py38.txt에서 관리
# Python 3.8 호환 버전으로 업데이트

# 공통 라이브러리 포함
-r requirements_common_py38.txt

# =============================================================================
# 음성 인식 전용 라이브러리 (Python 3.8 호환)
# =============================================================================
# 음성 인식 엔진
speechrecognition==3.10.0
pocketsphinx==0.1.15

# 한국어 음성 인식
# korean-speech-recognition==0.1.0

# =============================================================================
# 음성 처리 라이브러리
# =============================================================================
# 음성 신호 처리
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1

# 음성 특성 추출
python_speech_features==0.6

# =============================================================================
# 머신러닝 라이브러리
# =============================================================================
# 딥러닝 프레임워크 (Python 3.8 호환)
torch==2.1.1
torchaudio==2.1.1

# =============================================================================
# 한국어 처리
# =============================================================================
# 한국어 형태소 분석
konlpy==0.6.0
mecab-python3==1.0.6

# =============================================================================
# Python 3.8 호환성 확인
# =============================================================================
# 모든 라이브러리가 Python 3.8에서 안정적으로 동작
